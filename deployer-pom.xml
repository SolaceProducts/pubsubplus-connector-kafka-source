<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.solace.deploy</groupId>
  <artifactId>solace-deployer</artifactId>
  <name>Solace Deployer</name>
  <version>1.0.0</version>
  <description>A pom for publishing release version to maven central via Central Portal</description>
  <packaging>pom</packaging>

  <properties>
    <local.staging.dir>${project.build.directory}/localRepository</local.staging.dir>
  </properties>

  <repositories>
    <repository>
      <id>solace-internal</id>
      <releases>
        <enabled>true</enabled>
      </releases>
      <url>http://apps-jenkins:9090/nexus/content/groups/stable/</url>
    </repository>
  </repositories>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.0.0</version>
        <executions>
          <execution>
            <id>enforce-property</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireProperty>
                  <property>release.groupId</property>
                  <regex>^com.solace*(?:systems)?.*?</regex>
                  <regexMessage>release.groupId must be a valid group ID that starts with "com.solace".</regexMessage>
                </requireProperty>
                <requireProperty>
                  <property>release.artifactId</property>
                </requireProperty>
                <requireProperty>
                  <property>release.version</property>
                  <regex>^(?:\d+\.){2}\d+$</regex>
                  <regexMessage>release.version must be a non-SNAPSHOT semantic version.</regexMessage>
                </requireProperty>
                <requireProperty>
                  <property>local.staging.dir</property>
                </requireProperty>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>3.2.0</version>
        <executions>
          <execution>
            <id>regex-property</id>
            <goals>
              <goal>regex-property</goal>
            </goals>
            <configuration>
              <name>release.install.groupPath</name>
              <value>${release.groupId}</value>
              <regex>\.</regex>
              <replacement>/</replacement>
              <failIfNoMatch>true</failIfNoMatch>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-install-plugin</artifactId>
        <version>2.5.2</version>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <!-- Download artifacts from internal repository -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.2.0</version>
        <executions>
          <execution>
            <id>copy-artifacts</id>
            <phase>deploy</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <localRepositoryDirectory>${project.build.directory}/tmpRepository</localRepositoryDirectory>
              <artifactItems>
                <!-- Main JAR -->
                <artifactItem>
                  <groupId>${release.groupId}</groupId>
                  <artifactId>${release.artifactId}</artifactId>
                  <version>${release.version}</version>
                  <type>jar</type>
                  <overWrite>true</overWrite>
                  <outputDirectory>
                    ${local.staging.dir}/${release.install.groupPath}/${release.artifactId}/${release.version}/
                  </outputDirectory>
                </artifactItem>
                <!-- POM -->
                <artifactItem>
                  <groupId>${release.groupId}</groupId>
                  <artifactId>${release.artifactId}</artifactId>
                  <version>${release.version}</version>
                  <type>pom</type>
                  <overWrite>true</overWrite>
                  <outputDirectory>
                    ${local.staging.dir}/${release.install.groupPath}/${release.artifactId}/${release.version}/
                  </outputDirectory>
                </artifactItem>
                <!-- Sources JAR -->
                <artifactItem>
                  <groupId>${release.groupId}</groupId>
                  <artifactId>${release.artifactId}</artifactId>
                  <version>${release.version}</version>
                  <type>jar</type>
                  <classifier>sources</classifier>
                  <overWrite>true</overWrite>
                  <outputDirectory>
                    ${local.staging.dir}/${release.install.groupPath}/${release.artifactId}/${release.version}/
                  </outputDirectory>
                </artifactItem>
                <!-- Javadoc JAR -->
                <artifactItem>
                  <groupId>${release.groupId}</groupId>
                  <artifactId>${release.artifactId}</artifactId>
                  <version>${release.version}</version>
                  <type>jar</type>
                  <classifier>javadoc</classifier>
                  <overWrite>true</overWrite>
                  <outputDirectory>
                    ${local.staging.dir}/${release.install.groupPath}/${release.artifactId}/${release.version}/
                  </outputDirectory>
                </artifactItem>
                <!-- Gradle Module file -->
                <artifactItem>
                  <groupId>${release.groupId}</groupId>
                  <artifactId>${release.artifactId}</artifactId>
                  <version>${release.version}</version>
                  <type>module</type>
                  <overWrite>true</overWrite>
                  <outputDirectory>
                    ${local.staging.dir}/${release.install.groupPath}/${release.artifactId}/${release.version}/
                  </outputDirectory>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- Central Publishing Maven Plugin -->
      <plugin>
        <groupId>org.sonatype.central</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <version>0.7.0</version>
        <extensions>true</extensions>
        <configuration>
          <publishingServerId>central</publishingServerId>
          <autoPublish>true</autoPublish>
          <waitUntil>published</waitUntil>
          <waitMaxTime>3600</waitMaxTime>
          <checksums>all</checksums>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>2.7</version>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
